const sum = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
const g = 9.8;
const pi = 3.14159;

module.exports = {
  sum: sum, 
  subtract: subtract,
  multiply: multiply,
  divide: divide,
  g,
  pi
};